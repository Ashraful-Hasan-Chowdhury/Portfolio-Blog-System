<!--
<section class="main-slider">
    <ul class="bxslider">
        @foreach($slider as $post)
        <li><div class="slider-item">
            <img style="width:1140px;height:500px;" class="img-fluid" src="{{asset($post->image)}}">
            <h2>
                <a href="{{route('post',$post->slug)}}" title="{{$post->title}}">{{$post->title}}</a>
            </h2>
        </div>
    </li>
        @endforeach
    </ul>
</section> -->
